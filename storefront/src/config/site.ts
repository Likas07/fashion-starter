export const siteConfig = {
  featuredProducts: {
    count: 4, // Number of products to display in the featured section
  },
  // Add other site-wide configurations here as needed
}

export type SiteConfig = typeof siteConfig
